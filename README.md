# webkit-capturer
webkit-capturer
/*
usage

                var  chromeData = {/images url/}
                wkcapt.gui(gui);
                wkcapt.capture(chromeData, function (count) {
                    //step of capturing
                }, function () {
                    console.log('wkcapt finished!');

                });
*/
